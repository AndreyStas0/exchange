# 🚀 Система обміну ордерами - Backend версія (NeonDB)

Ця версія використовує Node.js backend + PostgreSQL (NeonDB) для синхронізації даних між пристроями.

## 📋 Що потрібно встановити

### 1. Node.js
Завантажте та встановіть Node.js: https://nodejs.org/ (версія 18 або новіша)

### 2. NeonDB акаунт
Створіть безкоштовний акаунт на https://neon.tech/

---

## 🔧 Налаштування NeonDB

### Крок 1: Створіть проєкт
1. Перейдіть на https://console.neon.tech/
2. Натисніть **"Create a project"**
3. Назвіть проєкт: `cabinet-exchange`
4. Оберіть регіон: **Europe (Frankfurt)** (найближчий до України)
5. Натисніть **"Create project"**

### Крок 2: Отримайте Connection String
1. Після створення проєкту ви побачите **Connection string**
2. Він виглядає так:
```
postgresql://username:password@ep-cool-name-12345.eu-central-1.aws.neon.tech/neondb?sslmode=require
```
3. **СКОПІЮЙТЕ** цей рядок!

---

## 💻 Налаштування проєкту

### Крок 1: Розпакуйте файли
Розпакуйте всі файли в одну папку, наприклад: `C:\cabinet-exchange\`

### Крок 2: Створіть файл .env
1. В папці проєкту створіть файл з назвою `.env` (без розширення)
2. Вставте туди:
```env
DATABASE_URL=your_connection_string_here
PORT=3000
```

3. Замініть `your_connection_string_here` на ваш Connection String з NeonDB

**Приклад:**
```env
DATABASE_URL=postgresql://neondb_owner:abc123def456@ep-cool-name-12345.eu-central-1.aws.neon.tech/neondb?sslmode=require
PORT=3000
```

### Крок 3: Встановіть залежності
Відкрийте термінал (Command Prompt або PowerShell) в папці проєкту і виконайте:

```bash
npm install
```

Це встановить всі необхідні пакети (express, pg, cors, dotenv).

---

## 🎯 Запуск системи

### Крок 1: Запустіть сервер
В терміналі виконайте:

```bash
npm start
```

Ви повинні побачити:
```
✅ Підключено до NeonDB
✅ База даних ініціалізована
🚀 Сервер запущено на http://localhost:3000
```

### Крок 2: Відкрийте frontend
Відкрийте файл `index.html` у браузері (просто двічі клікніть на нього)

### Крок 3: Тестуйте!
1. Оберіть кабінет
2. Створіть запит на ордери
3. Відкрийте `index.html` на іншому комп'ютері (або в іншому браузері)
4. Оберіть інший кабінет
5. Ви побачите запит і зможете з ним взаємодіяти! ✨

---

## 🌍 Доступ з інших комп'ютерів

### Для роботи в локальній мережі:

1. Дізнайтесь вашу IP адресу:
   - Windows: відкрийте Command Prompt і виконайте `ipconfig`
   - Знайдіть "IPv4 Address", наприклад: `192.168.1.100`

2. У файлі `index.html` (рядок ~470) змініть:
```javascript
const API_URL = 'http://localhost:3000/api';
```
на:
```javascript
const API_URL = 'http://192.168.1.100:3000/api';
```
(замініть на вашу реальну IP)

3. На інших комп'ютерах у вашій мережі відкрийте `index.html` - все працюватиме!

---

## 🚀 Розгортання в Інтернеті (необов'язково)

Щоб працювати з будь-якого місця через Інтернет, можна використати **безкоштовні** сервіси:

### Варіант 1: Render.com (рекомендовано)
1. Створіть акаунт на https://render.com/
2. Натисніть "New +" → "Web Service"
3. Підключіть GitHub репозиторій або завантажте файли
4. Налаштування:
   - Build Command: `npm install`
   - Start Command: `npm start`
5. Додайте змінну оточення `DATABASE_URL` (ваш NeonDB connection string)
6. Deploy!

Після деплою отримаєте URL типу: `https://your-app.onrender.com`

Замініть у `index.html`:
```javascript
const API_URL = 'https://your-app.onrender.com/api';
```

### Варіант 2: Railway.app
Схожий процес - безкоштовний тариф дає 500 годин роботи на місяць.

---

## 📁 Структура файлів

```
cabinet-exchange/
│
├── server.js          # Backend сервер (Node.js + Express)
├── index.html         # Frontend інтерфейс
├── package.json       # Залежності Node.js
├── .env              # Налаштування (DATABASE_URL)
├── .env.example      # Приклад налаштувань
└── README.md         # Ця інструкція
```

---

## 🔒 Безпека

⚠️ **ВАЖЛИВО:**
- Ніколи не публікуйте файл `.env` в Інтернеті
- Не передавайте свій Connection String іншим людям
- Для продакшн використання додайте авторизацію користувачів

---

## 🐛 Вирішення проблем

### Помилка: "Cannot find module"
```bash
npm install
```

### Помилка: "ECONNREFUSED"
- Перевірте чи запущений сервер (`npm start`)
- Перевірте чи правильний `API_URL` в `index.html`

### Помилка: "Connection refused" до NeonDB
- Перевірте чи правильний Connection String в `.env`
- Перевірте чи проєкт активний в NeonDB Console

### Сервер запустився, але frontend не підключається
- Перевірте чи правильний URL в `index.html` (рядок ~470)
- Якщо використовуєте Chrome, може потрібно дозволити CORS

---

## 💡 Корисні команди

```bash
# Встановити залежності
npm install

# Запустити сервер
npm start

# Запустити з автоматичним перезапуском (для розробки)
npm run dev
```

---

## 📞 Потрібна допомога?

1. Перевірте чи всі файли на місці
2. Перевірте чи `.env` файл правильно налаштований
3. Перевірте чи сервер запущений
4. Перевірте консоль браузера (F12) на наявність помилок

---

## ✨ Готово!

Тепер ваша система працює з **реальною базою даних** і синхронізується між всіма пристроями автоматично! 🎉

Дані зберігаються в NeonDB і доступні з будь-якого місця де запущений сервер.
